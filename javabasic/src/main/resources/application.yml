spring:
  application:
    name: cloud-javabasic #服务名称
server:
  port: 10080 #端口
eureka:
  instance:
    hostname: 127.0.0.1 #实例注册地址
  client:
    service-url:
      defaultZone: http://localhost:9000/eureka,http://localhost:9002/eureka/
